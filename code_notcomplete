// Example program
#include <iostream>
#include <string>
using namespace std;

template <class T>
struct Node{
    Node* left;
    T value;
    Node* right;
};


template <class T>
class Tree{
    Node<T>* head;
    
    public:
    
    Tree(){
        head = NULL;
    }
    
    void inserthelper(Node<T>*& parent, Node<T>* newnode){
        if(parent==NULL){
            parent = newnode;
        }
        else if(parent->value > newnode->value){
            inserthelper(parent->left, newnode);
        }
        else{
            inserthelper(parent->right, newnode);
        }
    }
    
    void insert(T data){
        Node<T>* newnode = (Node<T>*) malloc(sizeof(Node<T>));
        newnode->left = NULL;
        newnode->right = NULL;
        newnode->value = data;
        
        inserthelper(head, newnode);
    }
    
    void inorder_traversehelper(Node<T>* parent){
        if(parent!=NULL){
            inorder_traversehelper(parent->left);
            cout<<parent->value<<endl;
            inorder_traversehelper(parent->right);
        }else{
            return;
        }
    }
    
    void inorder_traverse(){
        cout<<"In inorder_traverse"<<endl;
        inorder_traversehelper(head);
    }
    
    void p(){
        cout<<head->right;
    }
    
    bool searchhelper(Node<T>* parent, T val){
        if(parent!=NULL){
            if(parent->value > val){
                if(searchhelper(parent->left, val)){return true;};
            }
            else if(parent->value < val){
                if(searchhelper(parent->right, val)){return true;};
            }
            else{
                return true;
            }
        }
        else{
            return false;
        }
    }
    
    bool search(T val){
        if(searchhelper(head, val)){
            return true;
        }
        return false;
    }
};


int main()
{
     Tree<int> n;
     n.insert(8);
     n.insert(10);
     n.insert(6);
     n.insert(7);
     n.insert(4);
     n.inorder_traverse();
     if(n.search(5)){
         cout<<"trye";}
         else{
             cout<<"asda";};
}
